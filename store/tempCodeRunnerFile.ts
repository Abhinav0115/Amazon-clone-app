                // const index = state.cart.findIndex(
                //     (item: { id: number }) => item.id === action.payload.id
                // );
                // state.cart[index].quantity += 1;
                // return;